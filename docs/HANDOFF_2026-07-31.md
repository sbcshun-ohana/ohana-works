# セッション引き継ぎ (2026-07-31)

前セッションのコンテキスト逼迫のため、新セッションでお知らせ Phase C を継続するための引き継ぎ。**このファイルを最初に読むこと。**

## 0. 最重要の前提(環境・接続)

- リポジトリ: `/Users/shuntakagi/Desktop/AI開発/ohana-works/ohana_works`、ブランチ **main**(main 直コミット運用・並行実装せず1本ずつ)。
- Supabase: **staging=`ulzachwkkyrvmxfzktio`(linked・PG17)** / **prod=`wdsziqxvmhwbdyfeiame`(未linked・PG17)**。
  - Claude が触れるのは **staging のみ**: `supabase db push --linked` / `supabase db query --linked "<SQL>"` / `supabase functions deploy <fn> --project-ref ulzachwkkyrvmxfzktio --use-api`。
  - **本番へは接続できない**(link 張替は事故リスクのため禁止)。本番作業は俊さんが実行。読み取り確認が要る時は SQL を用意して俊さんが実行。
- コミット末尾に必ず `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

## 1. 適用済みマイグレーション(staging)

| # | 内容 | 状態 |
|---|---|---|
| 145/146 | 園内記録(child_internal_notes)・volatility fix | staging適用済 |
| 147 | 統括園長(executive_director)・統括管理者(area_manager)+ multi_office_authority_grants + スコープ関数拡張 | staging適用済 |
| 148 | fetch_my_session_identity(氏名+最上位役職) | staging適用済 |
| 149/150/151 | staff_pins(PIN簡易ログイン)+ search_path/監査トリガ修正 | staging適用済 |
| 152/153 | guardian_notices(お知らせ Phase B)+ approve conflict fix | staging適用済 |
| 154 | fetch_my_session_identity に home_office_name 追加 | staging適用済 |
| **155** | お知らせ Phase C 職員閲覧系RPC(一覧 `fetch_guardian_notices_for_staff` / 宛先 `fetch_guardian_notice_targets` / 未読世帯 `fetch_guardian_notice_unread_recipients`) | **未適用・Phase C 検証時に適用可(適用時は §2 P2 の手順で 156 を一時退避し dry-run で155のみを確認してから)** |
| **156** | 園内連絡 Phase 2 DB(+ §9 申し送り廃止)※旧155をリネーム(中身不変) | **未適用・承認済み内容だが適用はお知らせ Phase C 完了報告後** |

- **本番はいずれも未適用**(prod は ~144 まで)。本番リリースは `docs/本番リリース手順書_統括園長_2026-07-31.md` の方針(お知らせ/園内記録が本番稼働する段階で 145〜 をまとめて1回・大原さんの統括園長切替も同枠)に従う。
- Edge Function(staging デプロイ済): `pin-staff-picker` / `pin-login`(要件3)。

## 2. 進行中タスクと優先順位

- **P1(最優先): お知らせ(保護者向け一斉配信)**。Phase B(DB=152/153)適用済・**拒否側E2E全件PASS**(下記)。**次は Phase C→D→E を連続実装**。これが完了するまで他機能のマイグレーション適用はしない。
- **P2: 園内連絡**。Phase 1(シフト構造調査)完了・俊さん承認済み。Phase 2 DB=**156(旧155をリネーム・未適用)**。**156 の適用はお知らせ Phase C 完了報告後**。
  - ⚠ **重要**: `supabase db push` は保留中を全件(155も156も)適用する。**リネームだけでは 156 の巻き込みは防げない**(dry-run実測で155・156の両方が保留表示)。155だけを適用するには、**適用時に 156 を一時退避**する:
    1. `mv supabase/migrations/20260714000156_staff_internal_messages.sql /tmp/`(退避)
    2. `supabase db push --linked --dry-run` で **155のみ**が対象であることを確認
    3. `supabase db push --linked`(155適用)
    4. `mv /tmp/20260714000156_staff_internal_messages.sql supabase/migrations/`(復元)
  - 上記手順は 2026-07-31 に dry-run で検証済み(退避時=155のみ表示・復元確認済み)。
- **P3: 統合追加機能 M1**。着手しない(お知らせ・園内連絡の DB 作業完了後に指示)。

### お知らせ Phase B E2E 結果(全件PASS・記録)
作成/承認の権限境界(chief=作成のみ/園長以上=承認/全施設=system_admin・統括園長のみ)、他施設配信不可、保護者は自分の approved+未取消+自分の既読のみ、他保護者の既読不可、preview の数字、未読世帯集計(any=父だけ既読でも世帯既読=`unread_children=0`)、取消後は保護者から不可視、承認者以外は取消不可 — **全て実アカウントJWTで実測PASS**。書込RPCは volatile 確認済み。

## 3. お知らせ Phase C 実装要件(admin_web 作成/承認画面)

**設計の正**: `docs/お知らせ_保護者向け一斉配信_調査設計_2026-07-30.md`(§4.2 画面配置・§6.1〜6.8・§6.5b 確認ダイアログ+取り消し・§8 影響範囲)。

- **新規ページ**(例 `/childcare/announcements`、ChildcareNav に「お知らせ(保護者向け)」)。職員向け `/notices` とは別物・混同回避(§4.3 で `/notices` は「職員向け」ラベル補強)。
- **宛先の階層複数選択**: 全施設 / 施設 / クラス / 園児。複数選択・階層併用可。全施設は他と排他。
- **状態フロー**: 下書き→申請(submit)→承認(approve=送信)/差戻し(return・理由必須)。作成者が園長以上なら draft→approve 直行。取り消し(revoke)=送信済みをアプリ内一覧から消す。
- **確認ダイアログ(必須・§6.5b)**: **approve ボタンの直前**に `preview_guardian_notice(notice_id)` の結果(施設名配列・保護者数・園児数・**「この操作は取り消せません(既送信プッシュは取り消せません)」**)を表示。承認する園長は宛先を選んでいないため配信規模をここで知る。revoke 実行時にも「既送信プッシュは取消不可」を明示。
- **既読表示**: 一覧に「既読 N/M名」。詳細に**未読世帯一覧**(判断#3=世帯/園児単位・any既読)。`fetch_guardian_notice_read_summary(notice_id)`。
- **権限でボタン出し分け**(実体はRPC): 作成=主任以上、承認/取消=園長以上、全施設作成=system_admin/統括園長。

**利用可能なRPC(152/153適用済)**: `create_guardian_notice(p_title,p_body,p_targets jsonb)` / `submit_guardian_notice(id)` / `approve_guardian_notice(id)` / `return_guardian_notice(id,reason)` / `revoke_guardian_notice(id,reason)` / `preview_guardian_notice(id)`→(office_names[],guardian_count,child_count) / `mark_guardian_notice_read(id)` / `fetch_guardian_notice_read_summary(id)`→(total_guardians,read_guardians,total_children,unread_children)。
- `p_targets` の形式: `[{"type":"all"}]` / `[{"type":"office","office_id":"…"}]` / `[{"type":"class","class_id":"…"}]` / `[{"type":"child","child_id":"…"}]`(複数・併用可)。
- **不足RPCの追加が要る** → **作成済み(マイグレーション155・未適用)**: 職員側の**お知らせ一覧取得RPC** `fetch_guardian_notices_for_staff(office)` + 宛先取得 `fetch_guardian_notice_targets(id)` + 未読世帯一覧 `fetch_guardian_notice_unread_recipients(id)` を `20260714000155_guardian_notices_staff_read_rpcs.sql` に追加。承認ゲートあり(適用は俊さん承認・Phase C検証時)。次に新規DBが要る場合の番号は157以降。
- Phase D=parent_app 表示(園全体/園児名バッジ・詳細到達で既読)、Phase E=プッシュ(`dispatch-pending-notifications` 拡張・園児名タイトル)は C の後。

## 4. 俊さんの判断待ち / 保留事項

- **お知らせ Phase C 着手**: E2E全PASS確認済のため着手OK(条件付き承認クリア)。
- **園内連絡 156(旧155)**: 中番=実在行化・facility母集団=`office_assigned_employee_ids` に単一化・PINピッカーは `office_accessible_employee_ids` へ寄せる、の修正を反映済み(commit `dd9618d`)。**適用はお知らせ Phase C 完了後**。番号は 155→156 にリネーム(中身不変)。
- **PINピッカーの共通化(提案)**: `pin-staff-picker` Edge Function を `office_accessible_employee_ids` RPC 呼び出しへリファクタ+再デプロイ=**155適用後の別作業**(在籍ロジックの二重化解消)。
- **本番シフト分布クエリ**(早番窓30分・19:00/20:00該当者・確定先行度): 俊さんが本番で実行して結果を貼る(§6.5、confirmed-only 切替判断用)。ステージングは shifts 0件。

## 5. 遵守事項(厳守)

1. **承認ゲート**: マイグレーションは適用前に**全文提示→俊さん承認**。本番書き込みは俊さんの明示承認後のみ。155 の適用はお知らせ Phase C 完了報告の後。
2. **検証規約**: 「ビルド通過」を「検証済み」と呼ばない。**拒否側E2Eを実アカウントのJWT context で実測**。手法: `supabase db query --linked "select set_config('request.jwt.claims', json_build_object('sub','<auth_user_id>','role','authenticated')::text, false); <検証SQL>"`。RLS直接SELECTの検証は `set local role authenticated;` を併用。実機確認(ブラウザ/iPad)は俊さんが行い、Claude は手順を箇条書き提示。
3. **フラグ運用**: 新機能は機能フラグ default OFF。施設別ONは `scripts/seed_staging.ts`(staging・冪等)。本番は都度リリース手順書。
4. **ドメイン分離**: 職員/保育業務/保護者の3ドメイン分離。`log_sensitive_access()` を呼ぶRPCに `stable` を付けない(volatile)。id列の無いテーブルに `log_event_change` 監査トリガを付けない(staff_pins の教訓)。
5. **番号衝突回避**: 新マイグno.は「適用時点の最新+1」。現状ローカル最新=156(155=お知らせPhase C RPC・156=園内連絡)。次は157。番号を文書にハードコードしない。

## 6. staging テストアカウント(お知らせ/権限E2E用)

- 職員: `stg-emp-0N+staging@ohana-works.test`(パスワードは俊さんが seed 時に設定した `STAGING_SEED_PASSWORD`)。
  - STG-EMP-01=統括園長(executive_director・全施設) / 02=大和chief / 04=system_admin / 05=統括管理者(BABY・Mahalo Station付与) / 03=大和staff。
- 保護者: `テスト保護者NN`。**兄弟/複数保護者3ケース**を seed 済(同一施設兄弟=保護者01↔園児01+09、別施設兄弟=保護者03↔園児03+10、1園児2保護者=園児05↔保護者05+06)。
- PIN端末: device_code `TEST-PIN-IPAD-BABY`(BABY MAHALO)。
- お知らせE2Eで作成したテスト通知が staging に残存(削除不要・テストデータ)。

## 7. 直近の主なドキュメント

- お知らせ: `docs/お知らせ_保護者向け一斉配信_調査設計_2026-07-30.md`
- 園内連絡: `docs/Ohana_Works_園内連絡_実装設計書_v1_2026-07-31.md`
- 統括管理者/セッション: `docs/統括管理者_調査報告_2026-07-30.md` / `docs/権限・セッション管理_設計案_2026-07-30.md`
- 本番リリース: `docs/本番リリース手順書_統括園長_2026-07-31.md`
- 統合追加機能(P3・着手しない): `docs/統合追加機能_PhaseA調査報告_2026-07-30.md` ほか
