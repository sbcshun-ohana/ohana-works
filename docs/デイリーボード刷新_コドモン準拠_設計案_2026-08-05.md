# デイリーボード刷新（コドモン準拠）設計案

作成: 2026-08-05 / 出典: 実機確認②の要望群（K5〜K8 + 登降園タイムバー + 出欠編集モーダル + 各種チェック一覧）とコドモン参照画面。

## 方針
デイリーボードを起点に、園児1人の当日の各機能（出欠・登降園・連絡帳・家庭での様子・健康/食事チェック）へ集約導線を張る。UI思想は「園児は一覧・操作は行内・時間軸/一括は上部」。

---

## 対象機能 × 現行データ対応表

| # | 画面/機能（コドモン参照） | 我々の既存データ | 判定 |
|---|---|---|---|
| 1 | **登降園タイムバー**（デイリーボード行内。実績=濃青 登園〜降園、予定=薄青、予定変更で下段合図、両端に時刻） | 実績: `child_attendance_events`(drop_off/pick_up の occurred_at)。予定: **無し**。RPCは `last_event_at` 1点のみ返す | **RPC改修(read-only)必須** ＋ 予定はDB追加 |
| 2 | **出欠編集モーダル**（✏️。出欠種別 -/遅刻/早退/病欠/都合欠・登降園予定・実績 入/外/戻/退・出欠メモ） | `child_daily_attendance`(is_absent + absence_reason)のみ。外出/戻りの event_type 無し | **DB拡張必須**（種別/予定/メモ列・event_type拡張・実績編集RPC） |
| 3 | **連絡帳ポップアップ**（名前下「日誌・連絡帳」。個人日誌備考＋公開設定＋保護者からの連絡） | `child_daily_contacts`(園側) ＋ `family_daily_reports`(保護者)：既存。連絡帳詳細画面・公開RPCも既存 | **主にUI**（行から既存詳細をポップアップ化） |
| 4 | **家庭での様子 一覧**（K8。機嫌/排便/睡眠/食事/検温/迎え/プール/子どもの様子/沐浴） | `family_daily_reports`：migration 123で全項目あり | **read-only fetch RPC 1本 ＋ 一覧UI**（schema変更なし） |
| 5 | **健康チェック・検温 一覧**（登園時/午睡明け等 複数回の体温＋時刻、一括） | `child_daily_contacts.temperature` + `temperature_measured_at`：**単一のみ** | 単一維持なら概ねUI。**複数回対応ならDB**（配列/子テーブル化） |
| 6 | **健康チェック・排便 一覧**（状態＋時刻、一括） | `child_daily_contacts.toileting_records jsonb`：あり | **UI ＋ bulk upsert RPC**（既存jsonbへ） |
| 7 | **午睡 一覧**（5分グリッド・一括） | `nap_sessions`/`nap_intervals`/`nap_checks`：**実装済**（共通時間軸・略記・一括はK1〜K3で対応済） | **実装済** |
| 8 | **食事チェック 一覧**（完食/ほとんど/だいたい＋保護者連絡＋画像） | `child_daily_contacts.meal_completion_pct`/`meal_free_note`：あり。献立は別 | **UI ＋ upsert RPC**（画像/献立は別途） |
| 9 | **登降園管理 一覧**（member_attendance/list。出欠連絡・出欠・予定(週次曜日別)・打刻実績・中抜け） | #1/#2 と同一データの一覧ビュー。予定は**園児ごとの曜日別 標準保育時間**由来 | **#2のDB拡張に含める＋一覧UI** |

---

## DB変更が必要（承認ゲート対象）
- **#2 出欠**: `child_daily_attendance` 拡張（`attendance_kind` enum・`scheduled_start_at`/`scheduled_end_at`/`scheduled_slot`・`attendance_note`）、`is_absent` は kind から生成列/トリガで同期（サマリー163非破壊）。`child_attendance_events.event_type` に `out`/`return` 追加。職員用RPC（出欠保存・実績修正）。
- **#1 登降園予定**: コドモンの登降園管理では予定が**曜日パターン（月火水木金 等）付き**で表示される＝**園児ごとの週次標準保育時間（曜日別）マスタ**が予定の既定値。よって二層構成を推奨: **(a) `child_weekly_schedule`（child × 曜日 × start/end）標準 ＋ (b) 日別override（#2の scheduled_* で当日変更）**。`fetch_daily_board_for_office` を drop+recreate し、登園/降園/外出/戻り時刻・予定(標準or当日)・種別・メモ・中抜け合計を返す（read-only）。
- **#9 登降園管理一覧**: #2/#1 と同一データの一覧編集ビュー（出欠連絡・出欠・予定・打刻・中抜け）。専用の追加スキーマは不要（#2＋週次スケジュールに乗る）。
- **#5 検温複数回**（対応する場合のみ）: `child_daily_contacts.temperature` を複数回対応（`temperature_records jsonb` 追加 等）。

## UI-onlyで先行可能（DB最小 or なし）
- **#3 連絡帳ポップアップ**（既存画面の再利用）
- **#4 家庭での様子 一覧**（read-only fetch RPC 1本追加のみ）
- **#6 排便 / #8 食事 の一覧入力**（既存 `child_daily_contacts` フィールドへの bulk UI ＋ upsert RPC）

---

## 段階計画案（提案）
- **Phase A（UI先行・DB最小）**: #4 家庭での様子一覧（RPC1本）＋ #3 連絡帳ポップアップ ＋ デイリーボードからの各機能導線集約。→ 早期に「一覧で見る」価値を提供。
- **Phase B（出欠・登降園）**: #2 DB拡張 → 出欠編集モーダル ＋ #1 登降園タイムバー（実績→予定の順）。K5(欠席選択廃止)を内包。
- **Phase C（健康/食事チェック一覧）**: #6 排便・#8 食事の一覧入力、必要なら #5 検温複数回。#7 午睡は実装済のため導線統合のみ。

## ロードマップ整合
本件は規模的に Phase 4 級。`docs/Ohana_Works_開発計画_改訂版_2026-07-17.md` と整合を取り、Phase 4（クラス活動+Instagram）との優先順位を俊と確定する。

---

## 俊への確認事項
1. **進める順序**: Phase A（家庭での様子一覧＋連絡帳ポップアップ）を先行でよいか。
2. **出欠(#2)**: `attendance_kind` 値（present/late/early_leave/sick_absence/personal_absence）で可か。遅刻/早退は「出席」に数えるか。`is_absent` 同期方式。実績 外出/戻りは1日1回想定か複数か。
2b. **予定(#1/#9)**: 週次標準保育時間（`child_weekly_schedule` 曜日別）＋日別override の二層で可か。標準スケジュールの登録UI（園児マスタ側）も要るか。「出欠連絡」欄は保護者申請（parent_requests）由来の表示でよいか。
3. **検温(#5)**: 単一維持か、複数回（配列/子テーブル）対応か。
4. **対象アプリ**: 各一覧は admin_web / Ohana Kids どちらを先行するか（両対応の順序）。
5. **既存の重複整理**: #7 午睡は実装済、#3/#4 は既存データ流用。新規は最小限に留める方針で可か。
