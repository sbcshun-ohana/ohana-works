# セッション引き継ぎ (2026-08-04)

前セッションからの継続。**このファイルを最初に読むこと。** 旧 `HANDOFF_2026-07-31.md` は本ファイルで置き換え(古い内容は参照しない)。次の着手対象は **新doc Phase 2(デイリーボード拡張)**。

## 0. 最重要の前提(環境・接続・規約)

- リポジトリ: `/Users/shuntakagi/Desktop/AI開発/ohana-works/ohana_works`、ブランチ **main**(main直コミット・1本ずつ直列)。
- Supabase: **staging=`ulzachwkkyrvmxfzktio`(linked・PG17)** / **prod=`wdsziqxvmhwbdyfeiame`(未linked)**。Claude が触れるのは **staging のみ**(`supabase db push --linked` / `supabase db query --linked "<SQL>"` / `supabase functions deploy <fn> --project-ref ulzachwkkyrvmxfzktio --use-api`)。**本番へは接続しない**(俊が実行)。
- コミット末尾に必ず `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- **承認ゲート厳守**: マイグレーションは適用前に**全文提示→俊承認**。番号は「適用時点の最新+1」(文書にハードコードしない)。フローは staging適用→拒否側E2E(実アカウントJWT)→俊の実機確認→本番。
- **検証規約**: 「ビルド通過」を「検証済み」と呼ばない。拒否側E2Eは `select set_config('request.jwt.claims', json_build_object('sub','<auth_user_id>','role','authenticated')::text, false); <SQL>` で実測。RLS直SELECT検証は `set local role authenticated;` 併用。
- **機能フラグ**: 新機能は default OFF。staging の施設別ONは `feature_flag_office_overrides` を upsert(汎用 `is_feature_enabled_for_office(key, office)`。薄いラッパー例: `is_child_internal_notes_enabled_for_office` / `is_childcare_home_enabled_for_office`)。試験施設=**BABY MAHALO**。
- **ドメイン分離**: 職員/保育業務/保護者の3ドメイン。`log_sensitive_access()` を呼ぶRPCに `stable` を付けない(volatile)。

## 1. 適用済みマイグレーション(staging)= **155〜167**

| # | 内容 |
|---|---|
| 155 | お知らせ Phase C 職員閲覧系RPC(一覧/宛先/未読世帯) |
| 156 | 園内連絡 Phase 2 DB(staff_messages系・staff_time_bands・class_homeroom・office_assigned/accessible_employee_ids)+ §9 申し送り区分廃止 |
| 157 | フラグ `parent_broadcast_notices`(お知らせ parent_app 表示) |
| 158 | `fetch_guardian_feature_flags` 許可リストに `parent_broadcast_notices` 追加 |
| 159 | フラグ `childcare_home_enabled`(Ohana Kids ホーム)+ `is_childcare_home_enabled_for_office` |
| 160 | お知らせ Phase E: approve→outbox生成(※on-conflictバグ、161で修正) |
| 161 | 160のon-conflict修正(153同様の列推論 `(notice_id,guardian_id,child_id)`) |
| 162 | **Phase2 §2.1** `fetch_childcare_classes` を年齢区分順(`age_group,class_name`)へ |
| 163 | **Phase2 §2.2** `fetch_daily_board_summary_for_office` 新設+`fetch_daily_board_for_office` に `class_id` 追加(id基準絞り込み) |
| 164 | **Phase2 §2.5** `daily_weather_records`+`upsert_daily_weather_record`(当日=施設アクセス保持者/過去日・未来日=主任以上) |
| 165 | **Phase2 §2.3** `record_staff_manual_attendance`(主任以上・proxy_*+実行職員ID・代理側のみ保護者通知=`childcare_attendance`・opt-out尊重) |
| 166 | **Phase2 §2.4** 連絡帳17時公開予約: `child_daily_contacts` に `scheduled_publish_at`/`published_at`・保護者SELECTを published_at基準へ・approve改修(承認=当日17:00既定予約)・schedule/publish_now/cancel RPC・cron `publish_due_daily_contacts`。type/category=`childcare_contact` |
| 167 | **Phase2 §2.4前提** `fetch_daily_board_for_office` に連絡帳公開状態4列(contact_id/status/scheduled/published)追加 |
| 168 | **Phase3 §3.6** nap_sessions/nap_checks + RLS + `childcare_classes.nap_check_required`(family_daily_report_required でseed) |
| 169 | **Phase3 §3** 午睡RPC(入眠/起床 個別・クラス一括、record_nap_check=30分ルール、変化なし複製、fetch_nap_board/fetch_nap_missing_slots) |
| 170 | **Phase3 §3.4** cron `detect_nap_check_gaps`(15:00 JST='0 6 * * *'・漏れ施設の管理者へ target_employee_id・office×date二重ガード) |

- **本番はいずれも未適用**(prod は ~144 まで)。**本番リリース範囲が 145〜170 に拡大**(未決§4参照)。
- staging override 済フラグ: `parent_broadcast_notices`=大和ON / `childcare_home_enabled`=BABY MAHALO ON。

## 2. 完了フェーズ(すべて staging 検証済)

- **お知らせ Phase B**(guardian_notices DB 152/153): 作成/承認/差戻し/取消・RLS。拒否側E2E全PASS。
- **お知らせ Phase C**(admin_web `/childcare/announcements` 作成/承認画面 + 155): ブラウザ9手順全PASS。
- **お知らせ Phase D**(parent_app 表示 157/158): ホーム「お知らせ」カード(フラグ安全側非表示)・一覧/詳細・既読(`mark_guardian_notice_read`)・園全体/園児名バッジ。シミュレータ6手順全PASS。プッシュtap→詳細遷移も結線済(`home_screen.dart`)。
- **お知らせ Phase E**(160/161 + dispatcher): approve=送信時に recipients→`notifications(target_guardian_id)` 生成。園児特定行タイトル`【〇〇ちゃん】`・all/office園児名なし・全角20字+「…」・payload(notice_id,child_id)・opt-out(`guardian_notification_settings` category='guardian_notice' OFF は push行不生成/recipientは残る=in-app可視)。dispatcher は既に guardian 配線済=機能変更なし(コメントのみ更新)。SQL E2E全PASS。**実FCM実機は俊**。
- **園内連絡 DB(156)**: staging適用+拒否側E2E全PASS(越境作成不可/非宛先ack不可/保護者拒否/RLS越境0/facility母集団=在籍のみ)。※**フロントUIは未実装**(Phase 2フロントは別途)。
- **クラスプルダウン共通化(Task 2・admin_web)**: `useChildcareClass`(?class=保持)/`childcareClassSort`/`ChildcareNav`が?office=+?class=引継ぎ。A(daily-board/contacts/contacts-copy/guardians/emergency-contacts)+B(children)+C(attendance/class-photos=?class=引継ぎ)。class-activitiesは副作用(ensure_class_daily_activity)のため除外。tsc/ESLint/next build クリーン。**並び順の正=`fetch_childcare_classes`返却順。Phase 2 §2.1で `order by cc.age_group, cc.class_name`(年齢区分順)へ改修=俊確定**。
- **ホーム Phase 1(Ohana Kids)**: `ChildcareHomeScreen`(7区分タイル)+ロゴ=ホームボタン(`OhanaLogoHomeButton`・全childcare画面)+名称「保護者申請」→「保護者からの連絡」(admin_web 2箇所)。`childcare_home_enabled` ON施設でホーム/OFFは従来メニュー(安全側)。dart analyze/tsc/next build クリーン。**iPad実機確認は俊**(BABY MAHALO・STG-EMP-05/pw `TestPhaseC2026!`)。
- **デイリーボード拡張 Phase 2(§2全部・162〜167)**: §2.1 クラス年齢区分順 / §2.2 在籍登園状況サマリー5項目(RPC集計・Realtime連動) / §2.3 代理登降園(主任以上・proxy_*・代理側のみ保護者通知) / §2.4 連絡帳17時公開予約(承認=当日17:00既定・cron公開+プッシュ・個別/クラス一括/施設一括・取消は不可視保持) / §2.5 天気記録。admin_web/Ohana Kids 両側実装。SQL E2E全PASS(拒否側/許可側/RLS/opt-out/バックフィル)。tsc/eslint/next build・dart analyze クリーン。**実機/ブラウザ目視は俊(下記§3-5)**。
- **午睡チェック Phase 3(§3全部・168〜170+フロント)**: 入眠/起床(個別・クラス一括)・5分グリッド記録(身体の向き4種+呼吸/顔色/寝具)・「5分前と同じ」複製(個別/クラス一括)・30分ルール(当日30分以内=一般職員/30分超・過去日=主任以上・source導出)・15:00 cron 漏れ検出→管理者プッシュ+デイリーボード警告バナー・admin_web 閲覧/修正(監査)。Ohana Kids ホームタイル結線。SQL E2E全PASS(30分境界29/31・過去日=主任以上・漏れ算出・二重ガード・拒否側RLS)。dart analyze/tsc/eslint/next build クリーン。commit `5604b4d`/`3af9cd4`。**実機/ブラウザ目視は俊(下記§3-6)**。

## 3. 俊の残タスク(実機・本番・分布)

1. **お知らせ Phase E 実FCM実機確認**: 実端末(FCMトークン登録済の保護者)でお知らせ承認→プッシュ受信(園児宛は`【〇〇ちゃん】`)→tap→詳細で既読。
2. **iPad実機確認バッチ**: (a) ホーム Phase 1(BABY MAHALO・7タイル/準備中出し分け/ロゴ→ホーム/フラグOFF施設は従来メニュー)、(b) 156 園内記録の**申し送り区分消滅**の目視(園内記録カテゴリが observation/guardian_contact/external_agency/other の4区分)。
3. **本番シフト分布クエリ3本**(early窓30分・19:00/20:00該当者・確定先行度): 俊が本番で実行し結果を貼る(園内連絡 band 判定 confirmed-only 切替の判断材料)。
4. 上記実機の結果報告(PASS/要修正)。
5. **デイリーボード拡張 Phase 2 の実機/ブラウザ目視(162〜167)**:
   - §2.1/2.2: admin_web/iPad でクラスが年齢区分順・サマリー5項目がクラス絞り込みに連動して正しい数字(欠席/登園/降園操作後に再確認)。
   - §2.3: 状態別「登園/降園を登録」ボタン→時刻手入力→登録が `staff_manual`(proxy_*)として保存・監査に残る。通知トグルONで**実FCM**で保護者に「○○が登園/降園しました」。
   - §2.4: 承認すると当日17:00予約バッジ・17:00に自動公開→**実FCMで保護者に連絡帳通知**・保護者アプリで公開まで不可視/公開後可視。時刻変更・即時公開・予約取消(取消後は不可視保持)・クラス一括/施設一括。
   - §2.5: 天気の当日入力/上書き(誰でも)・過去日は主任以上のみ・未入力表示。
6. **午睡チェック Phase 3 の実機/ブラウザ目視(168〜170)**:
   - Ohana Kids: クラス一括入眠→5分グリッド記録→「5分前と同じ」複製→起床の一連。30分ルール(29分は一般職員OK/31分は主任以上のみ)。園児追加(3歳+任意)。漏れセルの赤表示。
   - 15:00 cron: 実端末で漏れ施設の管理者へ**実FCM**プッシュ、デイリーボードに警告バナー。
   - admin_web `/childcare/nap`: 過去日を主任以上のみ修正でき、監査(source=admin_edit・checked_by)が残る。
   - **本番留意**: 170 cron `detect_nap_check_gaps` の pg_cron 稼働確認。seed は staging で `family_daily_report_required` 全true=全クラス nap_required true のため、**本番は 0〜2歳のみ必須になるようクラス別 `family_daily_report_required`/`nap_check_required` の curation が前提**。

## 4. 未決事項(俊の確定待ち)

- **Instagram 仮置き2点(§0.3 / §8-1,2)**: 投稿単位「施設ごと1日1投稿」・アカウント「OhanaGroup共通1」/ 投稿画像「クラスごと1枚選択」。**Phase 4着手前に確定必須**。
- **支援保育の担任作成権限**: 未確定(Phase 3/4関連で要確認)。
- **本番リリース範囲が 145〜167 に拡大**: `docs/本番リリース手順書_統括園長_2026-07-31.md` の想定(145〜)より対象が増えた。リリース枠・順序・大原統括園長切替の同枠実施を俊が再確定。166 の cron `publish_due_daily_contacts` は本番適用後に pg_cron 稼働確認、Vault の service key(dispatch)が本番に投入済であることも要確認。

## 5. 権限写像(実roles・§0.2/§6)

実 `roles`(sort_order昇順=上位): system_admin(10)/labor_manager(20)/executive_director=統括園長(30)/director=園長(40)/area_manager=統括管理者(50)/chief=主任(60)/office_manager=園管理者(70)/viewer(80)/staff(90)。

- **主任以上** = 既存 `manages_childcare(office)` を流用(system_admin/executive_director/director/chief/office_manager + area_manager は `multi_office_authority_grants` の付与施設のみ)。**labor_manager は保育業務権限に含めない(確定)**。
- **「管理者以上(主任除外)」区分は廃止=主任以上に統一(2026-08-04 俊確定)**。`is_childcare_admin` の新設は**不要**、`manages_childcare` を流用する。以前「管理者以上」を要するとしていた実装(§2.5 天気の過去日修正・§3.5 午睡30分超/過去日編集・§4.5 SNS可否)はすべて**主任以上**で実装する。
- **統括園長以上** = 既存 `is_executive_or_system_admin()`(executive_director/system_admin)。
- 「全施設お知らせ」作成/承認 = `is_executive_or_system_admin()`。施設/クラス/園児 = `manages_childcare(office)`。

## 6. 次アクション = 療育外出・戻り登録(新doc 2026-08-04)→ その後 職員マスタ開放 → Phase 4

**確定した実施順(2026-08-04 俊了承)**: ① Phase 3(午睡)**完了(168〜170+フロント・staging検証済・commit `5604b4d`/`3af9cd4`)** → ② **療育外出・戻り登録**(`docs/Ohana_Works_設計指示書_療育外出戻り登録_2026-08-04.md`・未決なし)→ ③ 職員マスタ部分開放(統括園長)→ ④ Phase 4(Instagram・着手時に投稿単位/投稿画像の2点確定を要求)。

**次=療育外出**: §4が Phase2実装済の `fetch_daily_board_for_office`(療育外出中バッジ列)と `fetch_daily_board_summary_for_office`(登園中=外出中を除外)を改訂する。**外出中導出ヘルパー→サマリー→board RPC を同一マイグレーションで一括改訂し drop+recreate を1回に抑える**。固定QR(revoke/再発行)・Edge Function `resolve-therapy-qr`(payload `therapy:`+token・保護者QRは不変)・機能フラグ `therapy_outing_enabled` 既定OFF・保護者通知なし・修正は主任以上・月次CSV/PDF出力。

### Phase 3(午睡)実装済メモ
設計の正: `docs/Ohana_Works_設計指示書_ホーム画面_デイリーボード拡張_午睡チェック_クラス活動Instagram_2026-08-03.md` §3。

- **§3 午睡チェック**(設計§3.1〜3.6): 対象園児/セッション・5分グリッド記録・「変化なし」1タップ複製・入眠/起床・**30分ルール(境界値29分/31分で正しく効く)**・15:00 cron 漏れ検出→デイリーボード警告+管理者プッシュ・**編集権限は§3.5(過去日/30分超=主任以上=`manages_childcare`。§4確定で「管理者以上」廃止)**・テーブル案§3.6。admin_web(監査/過去日編集)+Ohana Kids 両側。受入基準§7 Phase 3。
- **§2.4 補足(Phase 2で確定した実装挙動)**: 可視性は `published_at` 単独基準に統一(旧「scheduled NULL=即可視」は廃止)。**承認=当日17:00 既定予約を自動セット**(`approve_child_daily_contact` 改修・published_at NULL=公開まで不可視)→承認〜予約のギャップ無し。**予約取消は published_at 不変=不可視のまま保持**(即時公開にならない)。既存 approved 行は `published_at` バックフィルで可視維持。プッシュ type/category=`childcare_contact`。デイリーボードの連絡帳バッジは 167 の4列(contact_id/status/scheduled/published)から導出。
- 着手前に**新規テーブル/RPCの全文を提示→俊承認**(§3.6 午睡テーブル・15:00 cron 等)。§ごとにDB全文を1本ずつ直列提示。dart analyze/tsc/eslint/next build クリーン+フェーズ完了時 staging 検証を報告。

## 7. staging テストアカウント / 施設(E2E用)

- 施設ID: 大和=`c0a7010a-fd37-4cad-971b-6b3f2e80f842` / BABY MAHALO=`a7865932-08f1-4df4-bfe8-704d8cd9794b` / Mahalo Station=`c134824c-a4db-4744-95b8-c252e284ad88` / Halelea=`14ef175d-20e5-4ebc-a9e7-d3af3d026f10`。
- 職員: STG-EMP-01=テスト園長(大和, auth `5c3f81cb…`) / 02=テスト主任(大和, `0d8ae578…`) / 03=テスト副主任(大和, `b8326588…`) / 04=system_admin / 05=統括管理者(BABY/Mahalo付与, `7262fe7a…`, **pw `TestPhaseC2026!`**)。パスワード未設定時は `update auth.users set encrypted_password=crypt('<pw>',gen_salt('bf')) where email=…`。
- 保護者: `stg-guardian-0N+staging@ohana-works.test`。大和児の例=保護者02(guardian_id `3891d6ed…`, child `063a64c5…`)。兄弟/複数保護者3ケース seed 済。
- お知らせ/園内連絡のE2Eテストデータは staging に残置(削除不要)。

## 8. 参照ドキュメント / メモリ

- お知らせ: `docs/お知らせ_保護者向け一斉配信_調査設計_2026-07-30.md`(Phase C検証記録・§6.4プッシュ含む)
- 新doc(Phase 2〜4の正): `docs/Ohana_Works_設計指示書_ホーム画面_..._2026-08-03.md`
- v3.1統制版(§4のM2 OpenRouter/M4 R2整合): `docs/Ohana_Works_統合追加機能_実装統制設計書_v3_1_2026-07-30.md`
- 園内連絡: `docs/Ohana_Works_園内連絡_実装設計書_v1_2026-07-31.md`
- 本番リリース: `docs/本番リリース手順書_統括園長_2026-07-31.md`
- メモリ: `project_home_dailyboard_nap_instagram`(新doc計画/順序/権限写像)・`project_employees_exec_director_open`(職員マスタ部分開放=別キュー)・`project_dev_roadmap_2026-07`・`feedback_implementation_workflow`。
