# 本番リリース手順 145〜384(ドラフト v1・2026-08-28)

> **状態: ドラフト(俊レビュー待ち)**。Fable 4系統並列調査(145-230/231-310/311-384+環境)の統合。
> 旧手順書 `本番リリース手順_145-183_2026-08-05.md` の「第1弾=145-183+190/第2弾=184-197」区分は本書で置換する(旧書の §2.1退避・§3curation・共通注意は本書に引き継ぎ済み)。
>
> ⚠ **project ref を毎回目視確認**: 本番=`wdsziqxvmhwbdyfeiame` / staging=`ulzachwkkyrvmxfzktio`。
> ⚠ 実施タイミング: 登降園ピーク(7-10時/16-19時)と cron 実行時刻(JST 7:00/8:00/8:50/9:31/15:00/18:00)を避ける。

## 対象サマリ

| 項目 | 値 |
|---|---|
| 適用範囲 | migration **145〜384**(欠番: **179 / 266 / 322 / 380**) |
| 本番に流さない(恒久ホールド) | **347 / 348 / 349**(テストデータ・349は実データ破壊リスク) |
| 条件つき | **303**(大和UUID直書き→§2.2の分岐で書換の可能性) / **354**(マスタ+8/26テスト食数混在→適用後curationで削除) |
| storage 新設 | 6バケット: pickup-id-documents(202) / document-templates(205) / guardian-notice-attachments(208) / meal-menus(264) / meal-photos(300) / important-matters(310) |
| cron 最終形 | **11本**(§5の表)+ `compute-meal-counts` が**残っていない**こと |
| DB拡張 | btree_gist(171で導入)。pg_cron/pg_net は ~144 で導入済み想定(要確認) |
| Edge Functions | 16本すべて本番デプロイ(§1.2) |

---

## §0. 事前確認(最重要・分岐判定)

### 0.1 本番 offices の照合
本番SQLエディタで:
```sql
select id, office_code, name from offices order by office_code;
```
確認事項:
1. **office_code が 'O'/'M'/'S'/'H' の4値で設定されているか** — 156(時間帯連絡)/284(office_category)/307/308/339/360 が code 解決。未設定なら**先に設定**(curation)。
2. **name が「大和オハナ保育園」等 staging と同一表記か** — 254(給食行seed)が名前解決。
3. **id が staging のUUIDと一致するか**(HANDOFF_2026-08-04 に staging 4施設のUUID一覧):
   - 大和 = `c0a7010a-fd37-4cad-971b-6b3f2e80f842` ほか
   - → **一致** = 分岐A(303/329-331/335 をそのまま適用可)
   - → **不一致** = 分岐B(§2.2 の書換が必要)

### 0.2 childcare_classes の照合
```sql
select o.office_code, c.class_name, c.age_group from childcare_classes c join offices o on o.id = c.office_id order by 1,3;
```
- 254 はクラス名(はな/そら/かぜ/つき/ほし/にじ)完全一致依存(不一致=無言0行)。
- 282 は age_group「N歳」形式前提(不一致クラスは連絡帳必須=trueのまま残る)。
- 182 は staging UUID 直書きのため**本番では確実にno-op** → クラス実名化は §4 curation で実施。

### 0.3 vault / 拡張の確認
```sql
select name from vault.secrets;                          -- dispatch_notifications_service_key / _function_url があるか
select extname from pg_extension order by 1;             -- pg_cron / pg_net / pgcrypto(btree_gistは171で入る)
select jobname from cron.job order by 1;                 -- 適用前の既存ジョブ把握
```
- `dispatch_notifications_function_url` が**本番の** functions URL を指すこと(未設定/staging向きだと push 全停止)。

### 0.4 バックアップ
- Supabase ダッシュボードでバックアップ取得(またはPITR確認)を実施してから §3 へ。

---

## §1. 事前準備(DB以外)

### 1.1 Edge Functions デプロイ(16本)
```bash
supabase functions deploy --project-ref wdsziqxvmhwbdyfeiame
```
対象: analyze-menu-import / confirm-punch / dispatch-pending-notifications / generate-contact-note / generate-guidance-draft / issue-guardian-qr-token / issue-qr-token / pair-device / pin-login / pin-staff-picker / proxy-punch / resolve-guardian-qr / resolve-qr-punch / resolve-therapy-qr / send-push-notification(+_shared)。

### 1.2 Function secrets(ターミナル直接入力・チャット貼付禁止)
| Secret | 用途 | 状態 |
|---|---|---|
| `FCM_SERVICE_ACCOUNT_JSON` | push配信(dispatch/send/guardian-qr) | 必須 |
| `QR_TOKEN_SECRET` | 職員QR(issue/resolve-qr-punch) | 必須 |
| `GUARDIAN_QR_TOKEN_SECRET` | 保護者QR | 必須 |
| `ANTHROPIC_API_KEY` | AI3機能 | **方針どおり保留**(未設定=mock/fallback動作で安全) |

### 1.3 UI デプロイ準備
- admin_web: Vercel 本番デプロイは **DB適用完了後**(§7)。`next.config.ts` の outputFileTracingIncludes に attendance-pdf が入っていること(3f8949f)。
- Kids / parent_app: ビルド配布も DB 適用後。

---

## §2. 適用方式

**`supabase db push --linked`(ファイル名昇順一括)を推奨**。drop+recreate チェーン(163→167→172→186 / 197→201→202 / 318→324 / 346→352→362 / 353→361→363 / 235→236 / 381)が自動で正順になる。SQLエディタ手動適用にする場合は**絶対に番号順を崩さない**。

### 2.1 退避(ホールド)手順 — 旧§2.1踏襲
```bash
mkdir -p /tmp/ohana_hold
# 恒久ホールド(本番に流さない):
mv supabase/migrations/20260714000347_* /tmp/ohana_hold/
mv supabase/migrations/20260714000348_* /tmp/ohana_hold/
mv supabase/migrations/20260714000349_* /tmp/ohana_hold/
supabase db push --linked --dry-run   # 対象一覧を目視(347-349が無いこと・欠番4つ以外が揃うこと)
supabase db push --linked
# 完了後は必ず復元(repoの整合を壊さない):
mv /tmp/ohana_hold/* supabase/migrations/
supabase migration list --linked      # remote列で適用状態を確認
```
- 347=8/26テスト食数(officeが無いと**FK違反で停止**) / 348=実在児へテスト診断書 / **349=大和の child_allergy_diagnoses 全行を有効化するUPDATE(失効済み除去食の復活=給食事故リスク)**。3本とも本番不要のテストデータ。

### 2.2 UUID分岐(§0.1の結果で選択)
- **分岐A(本番officesのidがstagingと一致)**: そのまま適用可。182(クラス実名化)も有効に働くため §4.1 の一部は不要になる(適用後に件数確認)。
- **分岐B(不一致)**: 以下を適用前に書換(またはホールドして後日手動投入):
  - **303**(大和・保育安全計画穴埋め): `v_office := (select id from offices where office_code='O')` へ(307/308と同方式)。**書換なしで流すとFK違反で db push が停止**。
  - **329**(大和・全体/安全seed): 同上(**停止リスク**)。
  - **330/331/335**(月案8月9月/年間仮): 同方式へ書換(無言no-opになるだけだが、seedが入らない)。
  - **182**: 書換不要(no-op許容・§4.1 curationで代替)。

### 2.3 途中失敗時の注意
- **225(食材マスター)は再実行不可**(`version 1 投入済み` 例外で停止する再実行防止型)。145 の feature_flags INSERT もガード無し。**db push が途中失敗→リトライ**の際は、適用済み分を `supabase migration list` で確認し、必要なら repair(`supabase migration repair --status applied <ver>`)してから再開。

---

## §3. 適用の実施

1. §0 の確認・§1 の準備・§2.1 のホールドを済ませる。
2. `supabase db push --linked --dry-run` → 対象目視 → `supabase db push --linked`。
3. 所要目安: 236本・数分〜十数分。エラーで停止したら**番号を控えて**そのmigrationの節(§2.2/§2.3)を確認。

---

## §4. 適用後の本番データ curation(順序どおり)

### 4.1 クラス実名化(旧§3.0の引き継ぎ・分岐Bのとき必須/分岐Aは確認のみ)
- 旧手順書 §3.0 の施設名ベース INSERT/UPDATE(クラス6行追加・9行改名)を実施し、`select count(*)` で件数突合。
- 282 の効果確認: `select class_name, age_group, family_daily_report_required from childcare_classes;`(0-2歳=true/3-5歳=false になっているか)。

### 4.2 seed 0行チェック(無言no-op検知)
```sql
select count(*) from staff_time_bands;                        -- 156: 4施設×3band=12以上
select count(*) from meal_row_definitions;                    -- 254(大和)+339(M/S/H)+354: 数十行
select office_code, office_category from offices order by 1;  -- 284: O=authorized / M,S,H=corporate_led
select count(*) from guidance_plan_templates;                 -- 285/286/289/298/301: 10件以上
select count(*) from development_master_items;                -- 237: 90件
select count(*) from infectious_disease_masters where office_id is null;  -- 205: 22種
```
0行・NULL があれば §0 の名前/コード不一致 → 対象curationを実施。

### 4.3 テスト混在の掃除
```sql
-- 354が投入した 2026-08-26 の暫定食数を削除(マスタ行は残す)
delete from meal_count_rows where day_id in (select id from meal_count_days where business_date = '2026-08-26');
delete from meal_count_days where business_date = '2026-08-26';
```

### 4.4 374 の再実行(給食フラグON後・忘れると職員給食ゼロで稼働)
- 374 は「`meal_management_enabled` ON施設の在職職員に月〜金テンプレ付与」だが、**本番適用時は全施設OFFのため0行no-op**。
- §6 で給食フラグをONにした**後**に、374 の DO ブロックを SQLエディタで再実行 → `select count(*) from staff_meal_weekly_templates;` で職員数×5行程度を確認。

---

## §5. cron 確認(適用後)
```sql
select jobname, schedule from cron.job order by jobname;
```
**期待=11本**: monthly_paid_leave_auto_grant / monthly_attendance_summary / dispatch_pending_notifications(毎分) / publish_due_daily_contacts(毎分) / dispatch_scheduled_guardian_notices(毎分) / detect_nap_check_gaps(`0 9 * * *`=JST18:00版) / notify-open-incidents(`0 23 * * *`) / detect_stale_outings(`0 23 * * *`) / guidance_monthly_deadline_reminders(`0 22 * * *`) / materialize-meal-counts(`50 23 * * *`) / finalize-meal-counts(`31 0 * * *`)。
**不在確認**: `compute-meal-counts`(257→370でunschedule済)が残っていないこと。

## §6. 機能フラグの段階ON(リリース判断表)

全フラグ **default OFF**。ONは `feature_flag_office_overrides` へ施設別に挿入(機能フラグ画面 or SQL)。
- **例外=376 `attendance_mgmt_enabled` は grandfather で全施設ONになる**(migration適用時点の全offices)。初日から登降園管理ナビを全園に出したくない場合は適用後に override を削除/OFF化。
- 給食 `meal_management_enabled` ON時は **§4.4(374再実行)とセット**。
- `infection_gate_enabled` は `infection_control_enabled` とのANDゲート(gate単独ONは無効)。
- どのフラグをどの施設からONにするかは俊の運用判断(本書では強制しない)。

## §7. UIリリース(DB完了後・順序厳守: DB→UI)
1. admin_web を Vercel 本番へデプロイ → 主要画面スモーク(デイリーボード/登降園管理/給食/指導計画/**出席簿PDF・実績表PDF**=フォント同梱確認)。
2. Ohana Kids(iPad)/ parent_app のビルド配布。旧ビルドの取り残しに注意(315系の旧一時外出UIを含むビルドは廃止RPCを呼ぶ=HANDOFF 4-6)。
3. プッシュ実配信テスト(お知らせ1件→FCM着信)。

## §8. ロールバック方針(旧§5踏襲)
- 機能単位の縮退 = **フラグOFF / cron unschedule / Function secrets無効化** を第一手段とする(migration の逆適用はしない)。
- 適用途中停止時 = §2.3(repair→再開)。データ破壊系(349等)はそもそもホールド済み。

## §9. 未決事項(俊判断)
| # | 論点 | 選択肢 |
|---|---|---|
| 1 | 本番offices UUIDの一致有無(§0.1) | 一致=分岐A / 不一致=分岐B(303/329/330/331/335の書換) |
| 2 | 376 grandfather(全園に登降園管理) | そのまま / 適用後にOFF化して段階ON |
| 3 | 適用日・時間帯 | ピーク回避・当日中にUIデプロイまで完走できる枠 |
| 4 | フラグON順序(どの施設からどの機能を) | 運用判断(§6) |
| 5 | 347-349の恒久破棄の確認 | ホールドで確定(repoには残す) |
