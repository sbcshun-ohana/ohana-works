# 本番リリース手順 145〜384(ドラフト v1・2026-08-28)

> **状態: ドラフト(俊レビュー待ち)**。Fable 4系統並列調査(145-230/231-310/311-384+環境)の統合。
> 旧手順書 `本番リリース手順_145-183_2026-08-05.md` の「第1弾=145-183+190/第2弾=184-197」区分は本書で置換する(旧書の §2.1退避・§3curation・共通注意は本書に引き継ぎ済み)。
>
> ⚠ **project ref を毎回目視確認**: 本番=`wdsziqxvmhwbdyfeiame` / staging=`ulzachwkkyrvmxfzktio`。
> ⚠ 実施タイミング: 登降園ピーク(7-10時/16-19時)と cron 実行時刻(JST 7:00/8:00/8:50/9:31/15:00/18:00)を避ける。

## 対象サマリ

| 項目 | 値 |
|---|---|
| 適用範囲 | migration **145〜385**(欠番: **179 / 266 / 322 / 380**。385=園児マスタのクラス基準日修正・2026-08-28 staging検証済で追加) |
| 本番に流さない(恒久ホールド) | **347 / 348 / 349**(テストデータ・349は実データ破壊リスク) |
| ホールド→置換投入(§2.2) | **303 / 329 / 330 / 331 / 335**(大和UUID直書き。**分岐B確定=UUID置換してSQLエディタ投入+repair登録**) |
| 条件つき | **354**(マスタ+8/26テスト食数混在→適用後curationで削除) |
| storage 新設 | 6バケット: pickup-id-documents(202) / document-templates(205) / guardian-notice-attachments(208) / meal-menus(264) / meal-photos(300) / important-matters(310) |
| cron 最終形 | **11本**(§5の表)+ `compute-meal-counts` が**残っていない**こと |
| DB拡張 | btree_gist(171で導入)。pg_cron/pg_net は ~144 で導入済み想定(要確認) |
| Edge Functions | 16本すべて本番デプロイ(§1.2) |

---

## §0. 事前確認(最重要・分岐判定)

### 0.0 CLIリンクと migration 履歴のプリフライト ★事故防止の最重要チェック
背景: これまでmigrationはSQLエディタ手動適用のため、**履歴テーブルとの不整合が事故源**になる。
- ✅ **staging履歴は 2026-08-28 に修復済み**(184〜385の198本を repair で applied 登録・`migration list` で local-only/remote-only ともゼロ=完全一致390本を確認済み)。
- 当日の手順:
```bash
cat supabase/.temp/project-ref        # ① 現在のリンク先を目視(通常= ulzachwkkyrvmxfzktio)
supabase link --project-ref wdsziqxvmhwbdyfeiame   # ② 本番へリンク切替(DBパスワード要求あり)
supabase migration list --linked      # ③ 本番履歴の確認
```
- ③の**必須確認**: ✅**実施済み(2026-08-28・§0.2b)= remoteの最新は `20260714000139`・総数148(1〜139は穴なし)**。
  **140〜144(支援保育5本)がスキーマ適用済みなのに履歴未記録** → **push前に必ず以下を実行**(リンクを本番へ切替後):
```bash
supabase migration repair --status applied \
  20260714000140 20260714000141 20260714000142 20260714000143 20260714000144
supabase migration list --linked   # remote最新が 20260714000144 になったことを確認してから §2 へ
```
  - remote に 145以降が載っている場合: 想定外。**作業中断して状況確認**。
- ⚠ **リンク先がstagingのまま push すると、staging に145〜385の再適用を試みる大事故**(225例外停止・非冪等seed重複・349相当の再実行)。②の切替と③の「最新=144」確認を終えるまで push 禁止。
- 全作業完了後は必ず staging へ戻す: `supabase link --project-ref ulzachwkkyrvmxfzktio`

### 0.1 本番 offices の照合 — ✅実施済み(2026-08-28)→ **分岐B確定**
本番で `select id, office_code, name from offices order by office_code;` を実行した結果:

| 本番 id | code | name | staging id(参考) |
|---|---|---|---|
| `9296067b-8db3-438e-9d1a-c9d13d1d60be` | H | Halelea | `14ef175d-20e5-4ebc-a9e7-d3af3d026f10` |
| `f693e200-5312-44be-9634-6a8c24bc88da` | M | BABY MAHALO | `a7865932-08f1-4df4-bfe8-704d8cd9794b` |
| **`9503dfbd-6cdc-424c-aa3e-ac18441478ba`** | **O** | **大和オハナ保育園** | `c0a7010a-fd37-4cad-971b-6b3f2e80f842` |
| `493d0fce-bfb7-403c-9f93-12f3cb63aeb6` | S | Mahalo Station | `c134824c-a4db-4744-95b8-c252e284ad88` |

判定:
- **UUID = 全施設で不一致 → 分岐B**(303/329/330/331/335 は §2.2 の置換投入。182は本番でno-op=§4.1のcurationで代替)。
- **office_code = O/M/S/H 設定済み ✅** → 156/284/307/308/339/360 はそのまま安全。
- **施設名 = staging と同一表記 ✅** → 254(給食行seed)もそのまま安全。

### 0.2 childcare_classes の照合
```sql
select o.office_code, c.class_name, c.age_group from childcare_classes c join offices o on o.id = c.office_id order by 1,3;
```
- 254 はクラス名(はな/そら/かぜ/つき/ほし/にじ)完全一致依存(不一致=無言0行)。
- 282 は age_group「N歳」形式前提(不一致クラスは連絡帳必須=trueのまま残る)。
- 182 は staging UUID 直書きのため**本番では確実にno-op** → クラス実名化は §4 curation で実施。

### 0.2b 本番の読み取りプリフライト一括SQL(適用日前にいつでも実行可・すべて読み取りのみ)
**本番**SQLエディタで以下をまとめて実行し、結果を記録する:
```sql
-- (1) migration履歴の最新(期待: 20260714000144。空/145以上なら§0.0の分岐へ)
select version from supabase_migrations.schema_migrations order by version desc limit 3;
-- (2) 履歴の総数(期待: ~150前後=1〜144+初期)
select count(*) from supabase_migrations.schema_migrations;
-- (3) vault(期待: dispatch_notifications_service_key / _function_url の2行。無ければ§1で投入)
select name from vault.secrets order by name;
-- (4) 拡張(期待: pg_cron / pg_net / pgcrypto あり。btree_gist は171で入るので無くて良い)
select extname from pg_extension order by 1;
-- (5) 既存cron(適用前ベースライン。18本目標との差分確認用)
select jobname, schedule from cron.job order by jobname;
-- (6) 既存storageバケット(適用でも作られないので手動作成は不要=バケットはmigrationが作る)
select id from storage.buckets order by id;
-- (7) クラス構成(§0.2の照合)
select o.office_code, c.class_name, c.age_group from childcare_classes c join offices o on o.id=c.office_id order by 1,3;
```
**✅実施済み(2026-08-28)。結果の判定**:
- (1)=139・(2)=148 → **140〜144が履歴未記録** → §0.0のrepair 5本を当日必ず実行。
- (3)=2行あり ✓(当日、URL値の確認: `select decrypted_secret from vault.decrypted_secrets where name='dispatch_notifications_function_url';` → `https://wdsziqxvmhwbdyfeiame.supabase.co/functions/v1/...` を指すこと。staging URLなら更新)。
- (4)=pg_cron/pg_net/pgcrypto/btree_gist あり ✓ (5)=cron3本(ベースライン) ✓ (6)=バケット3個 ✓
- (7)=**S(Station)/H(Halelea)のクラスが本番未作成** → 339/354の給食行seedが無言0行になる。**§4.2の後に `docs/リリース用SQL_分岐B/クラス作成_S_H_curation.sql` を実行**(stagingの実構成から事前生成済み: S=リコ/ラウ/プア・H=ナル/カイ/モアナ 各2026年度・office_code解決・冪等。給食フラグONまでに)。実行後、339/354相当の給食行が入っているか §4.2 の `meal_row_definitions` 件数で再確認(0行なら339/354をSQLエディタで再実行)。Oは実クラス6・age_group「N歳児」形式で282の判定にも一致 ✓。

### 0.3 vault / 拡張の確認
```sql
select name from vault.secrets;                          -- dispatch_notifications_service_key / _function_url があるか
select extname from pg_extension order by 1;             -- pg_cron / pg_net / pgcrypto(btree_gistは171で入る)
select jobname from cron.job order by 1;                 -- 適用前の既存ジョブ把握
```
- `dispatch_notifications_function_url` が**本番の** functions URL を指すこと(未設定/staging向きだと push 全停止)。

### 0.4 バックアップ
- Supabase ダッシュボードでバックアップ取得(またはPITR確認)を実施してから §3 へ。

---

## §1. 事前準備(DB以外)

### 1.1 Edge Functions デプロイ(16本)
```bash
supabase functions deploy --project-ref wdsziqxvmhwbdyfeiame
```
対象: analyze-menu-import / confirm-punch / dispatch-pending-notifications / generate-contact-note / generate-guidance-draft / issue-guardian-qr-token / issue-qr-token / pair-device / pin-login / pin-staff-picker / proxy-punch / resolve-guardian-qr / resolve-qr-punch / resolve-therapy-qr / send-push-notification(+_shared)。

### 1.2 Function secrets(ターミナル直接入力・チャット貼付禁止)
| Secret | 用途 | 状態 |
|---|---|---|
| `FCM_SERVICE_ACCOUNT_JSON` | push配信(dispatch/send/guardian-qr) | 必須 |
| `QR_TOKEN_SECRET` | 職員QR(issue/resolve-qr-punch) | 必須 |
| `GUARDIAN_QR_TOKEN_SECRET` | 保護者QR | 必須 |
| `ANTHROPIC_API_KEY` | AI3機能 | **方針どおり保留**(未設定=mock/fallback動作で安全) |

### 1.3 UI デプロイ準備
- admin_web: Vercel 本番デプロイは **DB適用完了後**(§7)。`next.config.ts` の outputFileTracingIncludes に attendance-pdf が入っていること(3f8949f)。
- Kids / parent_app: ビルド配布も DB 適用後。

---

## §2. 適用方式

**`supabase db push --linked`(ファイル名昇順一括)を推奨**。drop+recreate チェーン(163→167→172→186 / 197→201→202 / 318→324 / 346→352→362 / 353→361→363 / 235→236 / 381)が自動で正順になる。SQLエディタ手動適用にする場合は**絶対に番号順を崩さない**。

### 2.1 退避(ホールド)手順 — 旧§2.1踏襲
```bash
mkdir -p /tmp/ohana_hold
# 恒久ホールド(本番に流さない):
mv supabase/migrations/20260714000347_* /tmp/ohana_hold/
mv supabase/migrations/20260714000348_* /tmp/ohana_hold/
mv supabase/migrations/20260714000349_* /tmp/ohana_hold/
# 置換投入ホールド(分岐B・§2.2で手動投入する5本):
mv supabase/migrations/20260714000303_* /tmp/ohana_hold/
mv supabase/migrations/20260714000329_* /tmp/ohana_hold/
mv supabase/migrations/20260714000330_* /tmp/ohana_hold/
mv supabase/migrations/20260714000331_* /tmp/ohana_hold/
mv supabase/migrations/20260714000335_* /tmp/ohana_hold/
supabase db push --linked --dry-run   # 対象一覧を目視(上記8本が無いこと・欠番4つ以外が揃うこと)
supabase db push --linked
# 完了後は必ず復元(repoの整合を壊さない):
mv /tmp/ohana_hold/* supabase/migrations/
supabase migration list --linked      # remote列で適用状態を確認
```
- 347=8/26テスト食数(officeが無いと**FK違反で停止**) / 348=実在児へテスト診断書 / **349=大和の child_allergy_diagnoses 全行を有効化するUPDATE(失効済み除去食の復活=給食事故リスク)**。3本とも本番不要のテストデータ。
- 303/329/330/331/335 = staging大和UUID直書きのため一括pushから外し、§2.2で置換投入する(303/329は不一致のまま流すと**FK違反でpushが停止**するため必須)。

### 2.2 分岐B確定: 303/329/330/331/335 の置換投入(db push完了後に実施)
§0.1 で **分岐B確定**(全施設UUID不一致・staging大和 `c0a7010a-…` → 本番大和 `9503dfbd-6cdc-424c-aa3e-ac18441478ba`)。
repoファイルは書き換えず、**UUID置換版をSQLエディタで手動投入 → repair で適用済み登録**する:

- ① 置換版SQLは**事前生成済み**: **`docs/リリース用SQL_分岐B/{303,329,330,331,335}_prod.sql`**
  (2026-08-28生成・検証済み: 各ファイルの差分は大和UUID 1行のみ/staging UUID残存なし/他のリテラルUUIDなし。当日のsed作業は不要)
- ② `303_prod.sql` から **番号順(303→329→330→331→335)** に本番SQLエディタへ貼って実行。
  - 前提順序: 301(安全計画テンプレ)・284-286(指導計画テンプレ)は db push で適用済みのこと(303/329はテンプレ不在だと exception で停止する設計)。
  - 330/331 の個人案は**児童名の文字列一致**で解決(本番は実データなので一致するはず。不一致児は raise notice でスキップ=ログ確認)。
- ③ 5本を remote 履歴に登録(次回 db push で再適用されないように):
```bash
for n in 303 329 330 331 335; do
  supabase migration repair --status applied 20260714000${n} --linked
done
supabase migration list --linked   # 5本の remote 列が applied であること
```
- **182**: 置換不要(本番ではno-op許容・§4.1 curationで代替)。
- **347/348/349 も repair で applied 登録する**(実行はしない=「意図的スキップ」。登録しないと以後の db push のたびにホールドが必要になり、忘れると349の事故リスクが再発するため、登録して恒久に封じる):
```bash
for n in 347 348 349; do
  supabase migration repair --status applied 20260714000${n} --linked
done
```
> 記録: 347/348/349 は本番では**一切実行していない**(テストデータのため意図的スキップ)。本書がその正式記録。

### 2.3 途中失敗時の注意
- **225(食材マスター)は再実行不可**(`version 1 投入済み` 例外で停止する再実行防止型)。145 の feature_flags INSERT もガード無し。**db push が途中失敗→リトライ**の際は、適用済み分を `supabase migration list` で確認し、必要なら repair(`supabase migration repair --status applied <ver>`)してから再開。

---

## §3. 適用の実施

1. §0 の確認・§1 の準備・§2.1 のホールドを済ませる。
2. `supabase db push --linked --dry-run` → 対象目視 → `supabase db push --linked`。
3. 所要目安: 236本・数分〜十数分。エラーで停止したら**番号を控えて**そのmigrationの節(§2.2/§2.3)を確認。

---

## §4. 適用後の本番データ curation(順序どおり)

### 4.1 クラス実名化(旧§3.0の引き継ぎ・分岐Bのとき必須/分岐Aは確認のみ)
- 旧手順書 §3.0 の施設名ベース INSERT/UPDATE(クラス6行追加・9行改名)を実施し、`select count(*)` で件数突合。
- 282 の効果確認: `select class_name, age_group, family_daily_report_required from childcare_classes;`(0-2歳=true/3-5歳=false になっているか)。

### 4.2 seed 0行チェック(無言no-op検知)
```sql
select count(*) from staff_time_bands;                        -- 156: 4施設×3band=12以上
select count(*) from meal_row_definitions;                    -- 254(大和)+339(M/S/H)+354: 数十行
select office_code, office_category from offices order by 1;  -- 284: O=authorized / M,S,H=corporate_led
select count(*) from guidance_plan_templates;                 -- 285/286/289/298/301: 10件以上
select count(*) from development_master_items;                -- 237: 90件
select count(*) from infectious_disease_masters where office_id is null;  -- 205: 22種
```
0行・NULL があれば §0 の名前/コード不一致 → 対象curationを実施。

### 4.3 テスト混在の掃除
```sql
-- 354が投入した 2026-08-26 の暫定食数を削除(マスタ行は残す)
delete from meal_count_rows where day_id in (select id from meal_count_days where business_date = '2026-08-26');
delete from meal_count_days where business_date = '2026-08-26';
```

### 4.4 374 の再実行(給食フラグON後・忘れると職員給食ゼロで稼働)
- 374 は「`meal_management_enabled` ON施設の在職職員に月〜金テンプレ付与」だが、**本番適用時は全施設OFFのため0行no-op**。
- §6 で給食フラグをONにした**後**に、374 の DO ブロックを SQLエディタで再実行 → `select count(*) from staff_meal_weekly_templates;` で職員数×5行程度を確認。

---

## §5. cron 確認(適用後)
```sql
select jobname, schedule from cron.job order by jobname;
```
**期待=11本**: monthly_paid_leave_auto_grant / monthly_attendance_summary / dispatch_pending_notifications(毎分) / publish_due_daily_contacts(毎分) / dispatch_scheduled_guardian_notices(毎分) / detect_nap_check_gaps(`0 9 * * *`=JST18:00版) / notify-open-incidents(`0 23 * * *`) / detect_stale_outings(`0 23 * * *`) / guidance_monthly_deadline_reminders(`0 22 * * *`) / materialize-meal-counts(`50 23 * * *`) / finalize-meal-counts(`31 0 * * *`)。
**不在確認**: `compute-meal-counts`(257→370でunschedule済)が残っていないこと。

## §6. 機能フラグの段階ON(リリース判断表)

全フラグ **default OFF**。ONは `feature_flag_office_overrides` へ施設別に挿入(機能フラグ画面 or SQL)。
- **例外=376 `attendance_mgmt_enabled` は grandfather で全施設ONになる**(migration適用時点の全offices)。初日から登降園管理ナビを全園に出したくない場合は適用後に override を削除/OFF化。
- 給食 `meal_management_enabled` ON時は **§4.4(374再実行)とセット**。
- `infection_gate_enabled` は `infection_control_enabled` とのANDゲート(gate単独ONは無効)。
- どのフラグをどの施設からONにするかは俊の運用判断(本書では強制しない)。

### 6.1 フラグON推奨マトリクス(CC提案のたたき台・俊が調整して確定)
方針: **職員側は広くON(staging実運用で枯れている)/保護者向けは大和から段階ON(実保護者のオンボーディングと同期)/未検証・運用未開始はOFF維持**。

| 区分 | フラグ | 推奨初期状態 | 根拠 |
|---|---|---|---|
| 職員コア | childcare_operations / childcare_home_enabled / child_internal_notes_enabled / class_messaging_enabled | **全施設ON** | staging全施設で日常運用済み |
| 職員コア | therapy_outing_enabled | 大和ON | 療育児がいる施設のみ |
| 登降園 | attendance_mgmt_enabled | **全施設ON**(376のgrandfatherを活かす) | 主任以上のみの管理画面・リスク小 |
| 登降園 | outing_reason_other_enabled | OFF維持 | 俊確定(療育+健診のみで運用開始) |
| 給食 | meal_management_enabled | **4施設ON + 直後に§4.4(374再実行)** | 厨房運用開始とセット |
| 給食 | meal_parent_section_enabled / meal_photo_push_enabled | OFF→保護者展開時 | 保護者向け |
| 記録系 | development_records_enabled / incident_reports_enabled / guidance_plans_enabled | **全施設ON** | 実機確認済み・現場が使う |
| 感染症 | infection_control_enabled | 大和ON→順次 | 書類様式PDFの投入とセット |
| 感染症 | infection_gate_enabled | OFF→運用合意後 | 登園ブロックは現場周知が前提 |
| 保護者 | guardian_app / attendance_qr / family_daily_report / communication_book / guardian_notices / guardian_requests / parent_broadcast_notices / class_photos | **大和のみON→段階** | 実保護者の招待・案内と同期 |
| 保護者 | medication_report_enabled / pickup_id_document_enabled | 大和ON(保護者群と同時) | 同上 |
| 入園 | enrollment_form_enabled / food_check_enabled | 全施設ON | 新入園の事務は即使う |
| 兄弟 | family_checkin_enabled | **OFF維持** | 実機通し確認が未了(既知) |
| 職員他 | employee_goal_sheet_enabled / bridge_curriculum_enabled / support_childcare_program_enabled | 現状どおり(すでに運用中の設定を踏襲) | 137系・勤怠側 |

## §7. UIリリース(DB完了後・順序厳守: DB→UI)
1. admin_web を Vercel 本番へデプロイ → 主要画面スモーク(デイリーボード/登降園管理/給食/指導計画/**出席簿PDF・実績表PDF**=フォント同梱確認)。
2. Ohana Kids(iPad)/ parent_app のビルド配布。旧ビルドの取り残しに注意(315系の旧一時外出UIを含むビルドは廃止RPCを呼ぶ=HANDOFF 4-6)。
3. プッシュ実配信テスト(お知らせ1件→FCM着信)。

## §8. ロールバック方針(旧§5踏襲)
- 機能単位の縮退 = **フラグOFF / cron unschedule / Function secrets無効化** を第一手段とする(migration の逆適用はしない)。
- 適用途中停止時 = §2.3(repair→再開)。データ破壊系(349等)はそもそもホールド済み。

## §9. 未決事項(俊判断)
| # | 論点 | 状態 |
|---|---|---|
| 1 | ~~本番offices UUIDの一致有無~~ | **✅解決(2026-08-28)= 分岐B確定**(§0.1に照合結果・§2.2に置換投入手順を確定) |
| 2 | ~~376 grandfather~~ | **✅確定(2026-08-28俊)= そのまま全施設ON**(主任以上のみの管理画面・リスク小) |
| 3 | ~~適用日・時間帯~~ | **✅確定(2026-08-28俊)= 日曜に実施**(全園休園で利用者影響ゼロ・直近=8/30想定。午前開始推奨: §0.0→§1→§2→§4-7を日中に完走) |
| 4 | ~~フラグON順序~~ | **✅方向確定(2026-08-28俊)= §6.1マトリクスの方針で進める**(職員側=広くON/保護者向け=大和から段階/未検証=OFF。細部は適用日までに調整可) |
| 5 | ~~347-349の恒久破棄~~ | **✅確定**: 実行せず repair で applied 登録(意図的スキップの正式記録=§2.2) |
