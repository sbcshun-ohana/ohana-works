# 引き継ぎ 2026-08-13 — 実機確認バッチ待ち

## 現状(DB/リリース)
- **staging = 145〜197 適用済み**(179 は欠番)。**本番 = ~144** のまま。
- リリース区分:
  - **第1弾 = 145〜183 + 190**
  - **第2弾 = 184〜197 + UI群**(手順書 `docs/本番リリース手順_145-183_2026-08-05.md` の §2.1=退避手順、§7=順序厳守。関数再作成migrationの共通注意は §2 に追記済み)
- staging=repoファイルと byte-faithful 整合を都度 `pg_get_functiondef` 照合で確認済み(193/194/195/196/197)。**197 は E2E 全7ケース PASS でDB側クローズ**。

## 未pushコミット一覧(origin/main より 7 ahead・実機確認後にまとめてpush)
| commit | 内容 |
|---|---|
| `d312fde` | グループ1(iPad実機②): Y2(PINカードoverflow)/Y3(ロゴ横overflow)/戻る導線(OhanaBackHomeLeading・下層4画面)/療育タブのフラグ判定 |
| `3f990a0` | グループ2: childcare時刻入力を時/分プルダウン共通化(showTimeDropdownPicker)+検温体温0.1刻みプルダウン |
| `d805367` | グループ3: K6タイムバーを1日固定の絶対時間軸(7:00-19:00)に再設計(Kids/admin_web両方) |
| `9793797` | 週次予定のトップ導線を追加(childcareホームタイル) |
| `b5ae5f7` | 排便記録UI(194の3RPC・健康チェック画面)+family-reports 園側検温列の削除(K8) |
| `90f949d` | 保護者の期間欠席+欠席種別 フロント(197対応。parent_app フォーム/一覧/詳細・admin_web承認画面) |
| `677afce` | housekeeping(.gitignore /tmp/・手順書2行) |
| `6b69d0e` | migration 193-197 をコミット(staging適用・E2E検証済・第2弾リリース対象) |

## migration 193-197(staging適用済・E2E検証済) = `6b69d0e` でコミット済み
- 193 連絡帳 申請ゲート緩和(assignee本人 OR manages_childcare)+assignee フォールバック
- 194 排便記録RPC(add/delete/fetch_toileting_record)
- 195 K8 公開時に検温欄へ最新値+CHECK 34.0緩和
- 196 K8 公開時に本文末尾へ当日検温全件スナップショット追記
- 197 保護者 期間欠席(end_date)+欠席種別(absence_kind)+承認時ボード自動反映(在籍クリップ)
- pg_get_functiondef照合済(193/194/195/196/197)・197はE2E全7ケースPASS。push対象に含まれる。

## 待機事項(最優先=俊の実機確認バッチ→指摘対応)
実機で以下を確認し、指摘が出たら対応(最優先):
- **G1**: Y2 PINカード縞消失 / Y3 全画面ロゴ横縞消失 / 戻る導線(園児詳細・クラス活動詳細・連絡帳詳細・家庭での様子一覧で1つ前へ戻れる) / 療育タブ(全施設OFFで非表示)
- **G2**: childcare 時刻入力が時/分プルダウン(検温・午睡・デイリーボード各所) / 検温体温が0.1刻みプルダウン
- **G3(K6)**: デイリーボードのタイムバーが1日固定軸で園児比較可能(Kids/admin_web)
- **週次予定**: ホーム「週次予定」タイル→デイリーボード→園児→週次アイコン到達
- **排便UI**: 健康チェック画面の排便ボタン→時/分+性状で追加/削除、連絡帳排泄欄と同一データ
- **197**: 保護者アプリで期間+種別欠席を申請→admin_web承認画面で期間/種別表示→承認→**デイリーボードで期間各日が欠席表示・163サマリー算入**(病欠/都合欠のkindも反映)。kind=NULL(旧アプリ)承認はボード反映なし。
- 実機確認で全PASSなら **未pushコミットをまとめて push**。

## 次のキュー(実機確認バッチ後)
- **C1〜C3(実FCM・保護者宛)**: お知らせ/代理登降園/連絡帳公開。実FCM基盤は開通済(dispatcher→dispatch-pending-notifications→FCM)。同経路で通せる。
- **第1弾リリース準備**(145〜183+190・本番~144→適用)。手順書の退避(§2.1)・順序(§7)厳守。

## 参照メモリ
`project_c4_push_y1_status`(iPad②/193-197状況)・`project_ai_pr_engine`(AI広報エンジン構想・Phase0待ち)・`feedback_implementation_workflow`(9ステップ・DBゲート)。
